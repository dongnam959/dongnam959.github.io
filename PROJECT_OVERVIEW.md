# 공정 설비 정보관리 시스템 — 프로젝트 개요

> 후공정 생산기술팀 · e-AWD 70kW MOTOR HOUSING ASS'Y 라인
> 최종 업데이트: 2026-08-31 (커밋 `bb65f26` 기준)

이 문서는 현재 구축되어 있는 "공정 설비 정보관리 시스템"의 구조·설계·운영 방식을 상세히 기록한 기술 참고 문서입니다. 향후 유지보수나 확장 작업 시 이 문서를 먼저 참고하면 됩니다.

---

## 1. 프로젝트 목적

설비에 부착한 QR코드를 스마트폰으로 스캔하면, 해당 설비의 작업표준·검사규격·일상점검표·트러블슈팅 이력 등을 모바일 웹페이지로 즉시 확인할 수 있게 하는 시스템. 종이/PDF 문서를 캐비닛에서 찾아야 하는 기존 방식을 대체하는 것이 목표.

---

## 2. 배포 구조 (호스팅)

| 항목 | 내용 |
|---|---|
| 저장소(repository) | [`dongnam959/dongnam959.github.io`](https://github.com/dongnam959/dongnam959.github.io) |
| 브랜치 | `main` (단일 브랜치, 별도 dev/staging 브랜치 없음) |
| 배포 방식 | GitHub Pages (저장소 이름이 `<계정>.github.io` 형식이면 자동으로 루트 URL에 배포됨) |
| 실서비스 주소 | https://dongnam959.github.io/ |
| 빌드 도구 | 없음 — 순수 정적 HTML/CSS/JS 파일. `main`에 push되면 수 분 내 자동 반영 |
| 프레임워크 | 없음 (React/Vue 등 미사용, 바닐라 HTML + 인라인 `<style>` + 바닐라 JS) |
| 로컬 개발 환경 | `C:\Users\user\Desktop\후공정 설비 QR 코드 스마트폰 관리 시스템\후공정 설비 정보 시스템 프로젝트` |

배포 절차는 `git add` → `git commit` → `git push origin main` 세 단계뿐이며, 별도 배포 서버·CI 파이프라인이 없습니다.

---

## 3. 사이트맵 (전체 11개 페이지)

공정 흐름(마킹 → 조립전 검사 → 조립 → 조립후 검사·최종검사) 순서로 정렬.

| 공정No. | 설비명 | 파일명 | 문서번호 | 아코디언 섹션 수 |
|---|---|---|---|---|
| — | (홈/메뉴) | `index.html` | — | 해당 없음 |
| 110 | 레이저 마킹기 | `laser-marking.html` | DnK-250915-001 | 7 |
| 120 | 조립전 유로계 리크검사기 | `leak-pre-flow.html` | DnK-250915-002 | 9 |
| 130 | 조립전 전체계 리크검사기 | `leak-pre-full.html` | DnK-250915-003 | 9 |
| 140 | 오일니쁠 압입기 / 테이퍼 플러그 체결기(1개소) | `nipple-oil-press.html` | DnK-250915-004 | 7 |
| 150 | 씰캡 압입기 | `cap-sealing-press.html` | DnK-250915-005 | 7 |
| 160 | 테이퍼 플러그 체결기(5개소) | `taper-plug-fastening.html` | DnK-250915-006 | 7 |
| 170 | 다월핀 압입기 | `dowel-pin-press.html` | DnK-250915-007 | 7 |
| 180 | 조립후 유로계 리크검사기 | `leak-post-flow.html` | DnK-250915-008 | 9 |
| 190 | 조립후 전체계 리크검사기 | `leak-post-full.html` | DnK-250915-009 | 9 |
| 200 | 테이퍼 플러그 돌출높이 검사기 | `taper-plug-height.html` | DnK-250915-010 | 7 |

> `leak-pre-flow.html`은 원래 이 프로젝트의 최초 파일인 `index.html`이었습니다. 홈/메뉴 페이지를 새로 만들면서 `leak-pre-flow.html`로 이름을 옮기고, `index.html`은 전체 목록 페이지로 새로 제작했습니다 (2026-07-30, 커밋 `8a49c52`).

---

## 4. 디자인 시스템

모든 페이지가 하나의 CSS를 그대로 복사해서 재사용합니다 (별도 공용 CSS 파일 없이 각 `.html` 파일 `<style>` 태그 안에 동일한 내용이 중복 포함되어 있음 — 빌드 도구가 없어 의도적으로 이렇게 구성).

### 색상 팔레트 (CSS 변수)

| 변수 | 값 | 용도 |
|---|---|---|
| `--navy` | `#0a2540` | 브랜드 메인 컬러, 상단 바·헤더·강조 요소 |
| `--navy-2` | `#0f3255` | 보조 네이비 |
| `--steel` | `#1a4971` | 보조 |
| `--accent` | `#0f6cb0` | 타임라인 강조선 |
| `--gray-900`~`--gray-100` | `#181d22` ~ `#f4f6f8` | 본문/보조 텍스트, 배경 단계별 회색조 |
| `--ok-bg` / `--ok-text` | `#eaf4ee` / `#1c6b41` | 정상·완료 상태 태그 |
| `--warn-bg` / `--warn-text` | `#fbf3e2` / `#8a5a12` | 주의·경고 콜아웃 |
| `--line` | `#d3d8de` | 테두리/구분선 |

이 팔레트는 실제 DnK 모빌리티 로고(네이비+시안) 톤에 맞춰 선정했으며, 이후 PPT 보고자료 표지 및 접속 암호 화면 디자인에도 동일하게 사용했습니다.

### 타이포그래피 & 레이아웃

- 폰트: `-apple-system, BlinkMacSystemFont, "Malgun Gothic", "Apple SD Gothic Neo", sans-serif`
- 뷰포트: `max-width: 640px` 중앙 정렬 — 모바일 브라우저 기준으로 설계 (데스크톱에서도 문제없이 보이지만 최적화 대상은 스마트폰)
- 반응형 메타 태그로 확대/축소 제한 (`maximum-scale=1.0`)

### 공통 레이아웃 구조 (위→아래)

1. **`.id-bar`** — 네이비 바탕 최상단 바. `DnK MOBILITY · 후공정 생산기술팀` / `e-AWD 70kW MOTOR HOUSING ASS'Y`
2. **`.doc-header`** — 네이비 배경, 문서 라벨(`공정 설비 정보관리 시스템 · EQUIPMENT INFORMATION MANAGEMENT SYSTEM`) + 페이지 제목(h1) + `.title-block`(흰 배경 요약표)
3. **`.title-grid`** — 2열 그리드 표. 공정명/공정No.(전체너비) → 품명 → 품번 → 설비명 → 문서번호 → 개정일자 → 상태 → 설비별 핵심 파라미터 4칸
4. **`<main>` 아코디언 섹션들** — `<details class="section">` (기본 닫힘 상태), 원 번호 아이콘(①~⑨) + 제목 + 화살표(▾)
5. **`<footer>`** — 브랜드명 + 안내 문구

### 아코디언 섹션 구성 (기본 7단계, 리크테스터는 조작매뉴얼+이력 항목 추가로 9단계)

| 번호 | 섹션명 | 비고 |
|---|---|---|
| ① | 설비 개요 | 설비관리번호(공란)·문서버전·점검주기·최종업데이트. 개인 이름 미기재 |
| ② | 첨부 문서 | PDF 링크 3~4개 (§6 참고) |
| ③ | 표준 작업 순서 | 번호 타임라인(`.flow`) + 안전주의사항 + 이상발생시 조치 + 부적합품 처리 |
| ④ | 검사 규격 및 파라미터 | 표 (항목/규격/주기) |
| ⑤ | 설비 일상점검표 체크 항목 | 전원부/구동부/안전장치/공정특성 구분, 주간·야간 체크란 |
| ⑥ | 조작 매뉴얼 요약 | **리크테스터(LS-R700/LS-R902) 설비 4개소만 존재** (120·130·180·190) |
| ⑦ | 트러블슈팅 이력 | 실제 사내 이슈 기록 있는 설비(120·130·180·190)만 실사례 기재, 나머지는 "등록된 사례 없음" |
| ⑧ | 마스터 샘플 · 보정 이력 | **리크테스터 설비 4개소만 존재** (120·130·180·190). 일자·샘플명·리크값(mL/min)·함침여부·Fixed comp value·설비주변온도·판정·확인자·비고를 가로 표로 계속 누적 기록 (2026-08-31 추가, `table.hist`). 비고 열은 좁게, 나머지는 `min-width:64px`로 넓게 고정 — 헤더가 한글이라 줄바꿈이 자유로우면(`white-space:normal`) 브라우저가 글자 단위로 줄바꿈해 열 전체가 찌그러지는 문제가 있어 강제함 |
| ⑨ | 최근 공지 | 현재 전 설비 "등록된 공지 없음" (수시 업데이트 영역) |

> 향후 계획: 설비전기회로도·기계도면 등 새 항목을 추가할 예정이며, 추가 후 항목별로 §8 접속 제한을 적용할지 개별 결정할 예정 (§11 참고).

---

## 5. 홈 화면 (`index.html`) 구성

기존 설비 상세 페이지와 다른 전용 레이아웃(아코디언 없음, 카드 리스트 형태):

- 상단 통계 칩 3개: 등록 설비(10개소) / 공정 범위(No.110~200) / 문서번호(DnK-250915-001~010)
- 공정 흐름별 그룹 헤더 4개: **마킹 → 조립전 리크검사 → 조립 → 조립후 검사·최종검사**
- 각 그룹 안에 `.eq-card` 카드 리스트 (공정No. 배지 + 설비명 + 공정명 + 화살표), 클릭 시 해당 설비 페이지로 이동

---

## 6. 첨부 문서 (PDF) 체계

각 설비 페이지 "② 첨부 문서" 섹션에서 아래 파일명을 프로젝트 루트에서 직접 찾습니다 (하위 폴더 없음).

| 설비 | 필요 PDF 파일명 | 현재 상태 |
|---|---|---|
| `leak-pre-flow.html` | `partlist.pdf`, `machine-spec.pdf`, `manual.pdf`, `ls-r700-manual.pdf` | ✅ 4개 모두 저장소에 존재 (최초 업로드분) |
| `laser-marking.html` | `laser-marking-partlist.pdf`, `laser-marking-spec.pdf`, `laser-marking-manual.pdf` | ⬜ 미업로드 |
| `leak-pre-full.html` | `leak-pre-full-partlist.pdf`, `leak-pre-full-spec.pdf`, `leak-pre-full-manual.pdf`, `ls-r700-manual.pdf`(공용) | ⬜ 미업로드 |
| `nipple-oil-press.html` | `nipple-oil-partlist.pdf`, `nipple-oil-spec.pdf`, `nipple-oil-manual.pdf` | ⬜ 미업로드 |
| `cap-sealing-press.html` | `cap-sealing-partlist.pdf`, `cap-sealing-spec.pdf`, `cap-sealing-manual.pdf` | ⬜ 미업로드 |
| `taper-plug-fastening.html` | `taper-fastening-partlist.pdf`, `taper-fastening-spec.pdf`, `taper-fastening-manual.pdf` | ⬜ 미업로드 |
| `dowel-pin-press.html` | `dowel-pin-partlist.pdf`, `dowel-pin-spec.pdf`, `dowel-pin-manual.pdf` | ⬜ 미업로드 |
| `leak-post-flow.html` | `leak-post-flow-partlist.pdf`, `leak-post-flow-spec.pdf`, `leak-post-flow-manual.pdf`, `ls-r700-manual.pdf`(공용) | ⬜ 미업로드 |
| `leak-post-full.html` | `leak-post-full-partlist.pdf`, `leak-post-full-spec.pdf`, `leak-post-full-manual.pdf`, `ls-r902-manual.pdf` | ⬜ 미업로드 |
| `taper-plug-height.html` | `taper-height-partlist.pdf`, `taper-height-spec.pdf`, `taper-height-manual.pdf` | ⬜ 미업로드 |

링크는 이미 페이지에 걸려 있으므로, 위 파일명 그대로 PDF를 프로젝트 루트에 넣고 `git add`/`commit`/`push`만 하면 "② 첨부 문서" 버튼이 즉시 작동합니다.

### 뷰어 (다운로드 버튼 없이 보기 전용, 2026-08-31 추가)

"② 첨부 문서" 링크는 PDF 파일을 직접 열지 않고 `pdf-viewer.html?file=파일명.pdf` 형태로 감싸서 엽니다.

- `pdf-viewer.html`: `pdfjs/pdf.min.mjs`(vendored, `pdfjs-dist` npm 패키지의 빌드 산출물)로 PDF 각 페이지를 `<canvas>`에 직접 그려서 보여주는 자체 제작 뷰어. 다운로드·인쇄·저장 버튼을 아예 두지 않았고, 우클릭(길게 누르기) 메뉴도 막아둠.
- `gate.js`도 이 페이지에 함께 걸려 있어, 새 탭으로 PDF 링크를 열면(예: 다른 세션에서 직접 접근) 다시 암호를 확인합니다.
- **완전한 다운로드 차단은 아닙니다.** 파일 자체는 여전히 서버에 그대로 존재해서 주소를 알면 직접 받을 수 있고, 화면 캡처도 막을 수 없습니다. "저장 버튼을 없애 덜 눈에 띄게" 하는 수준입니다 (§8 암호 게이트와 동일한 성격의 트레이드오프).
- 파일 용량: `pdf.min.mjs`(~350KB) + `pdf.worker.min.mjs`(~1.3MB), 최초 방문 시 1회 다운로드되고 이후 캐시됨.

---

## 7. QR 코드

- 위치: `qr-codes/` 폴더, PNG 11개 (홈 1개 + 설비 10개)
- 생성 방식: Python `qrcode` 라이브러리 (`qrcode.make(url, box_size=10, border=4)`)
- URL ↔ 파일 매핑: 각 PNG 파일명이 대응 `.html` 파일명과 동일 (예: `laser-marking.png` → `https://dongnam959.github.io/laser-marking.html`)
- `leak-pre-flow.png`는 index.html 개편 이후 추가로 재생성됨 (구 index.html 주소가 홈 화면으로 바뀌면서, 원래 그 페이지였던 조립전 유로계 리크검사기용 QR을 새 주소로 다시 생성)

QR 자체는 URL을 담은 이미지일 뿐이며 접근 제어 기능은 없습니다. 실제 접근 제한은 §8의 암호 게이트가 담당합니다.

---

## 8. 접속 제한 (암호 게이트, `gate.js`)

2026-08-17 추가, 2026-08-20 전체 잠금 방식으로 재설계. 새 계정·유료 서비스 가입 없이, 지금 쓰는 GitHub Pages 그대로 클라이언트(브라우저) JS만으로 구현한 간이 접근 제한 장치입니다.

### 동작 방식

- **11개 페이지(index.html 포함) 전부, 진입 즉시 화면 전체가 가려지고 암호 입력창이 뜹니다.** 설비명·요약표·섹션 제목 등 예외 없이 전부 숨김.
  - 스크립트가 `<body>` 최상단에서 동기 실행되며 `document.documentElement.style.visibility = "hidden"`을 콘텐츠가 그려지기 전에 적용하는 방식이라, 깜빡임 없이 즉시 가려집니다.
- 한 번 맞으면 그 페이지뿐 아니라 같은 탭에서 방문하는 나머지 페이지도 모두 즉시 열립니다.
- 인증 상태는 `sessionStorage`에 저장됩니다: **같은 탭에서 다른 페이지로 이동해도 유지**되지만, **탭/브라우저를 닫거나 QR을 새로 스캔해 새 탭으로 열면 다시 암호를 물어봅니다.**
- 암호는 SHA-256 해시로 비교합니다 (평문을 코드에 그대로 남기지 않기 위함).

> 이전 버전(2026-08-17~08-20)은 페이지 상단 요약표는 바로 보여주고 아코디언 섹션을 펼칠 때만 암호를 묻는 "부분 잠금" 방식이었습니다. 다만 (1) index.html은 아코디언 자체가 없어 이 로직이 전혀 걸리지 않아 완전 공개 상태였고, (2) 설비 페이지도 요약표·섹션 제목이 미리 보여 "잠금이 안 걸린 것처럼" 보인다는 피드백을 받아, index.html과 완전히 동일한 "전체 즉시 잠금" 방식으로 전 페이지를 통일했습니다.

### 파일 구조

- `gate.js` 파일 하나가 11개 페이지에서 공용으로 로드됨 (`<body>` 시작 직후 `<script src="gate.js"></script>` 한 줄씩 추가)
- 암호를 바꾸려면 `gate.js` 안의 `PASS_HASH` 값을 새 암호의 SHA-256 해시로 교체 (파일 상단 주석에 생성 명령어 있음)
- 현재 공유 암호: `dnkm8721` (변경 이력: 최초 `dnk0915` → 2026-08-17 `dnkm8721`로 교체)

### 한계 (의도적으로 받아들인 트레이드오프)

- **진짜 보안이 아님**: 브라우저 개발자도구로 `gate.js` 소스를 보면 로직과 해시를 확인할 수 있어, 마음먹으면 우회 가능합니다. 목적은 "QR 찍고 지나가다 우연히/장난으로 계속 들여다보는 것"을 막는 수준입니다.
- **개인별 구분 없음**: 공유 암호 1개로 전원이 동일하게 인증합니다. 개인 계정·열람 이력이 필요하면 별도 백엔드(Firebase 등)가 필요하며, 이는 정적 사이트 범위를 벗어나는 별도 개발 프로젝트입니다 (§11 참고).
- **항목별 세부 차등 없음**: 페이지 전체가 하나의 공유 암호로 잠깁니다. 특정 섹션/항목만 다르게 잠그거나 일부는 공개로 남기는 것은 §11 향후 계획(전기회로도·기계도면 추가 이후)에서 검토 중.

---

## 9. 원본 문서 정정 사항

작업표준서(DnK-250915-001~010) 원본의 오탈자를 QR 페이지에는 아래와 같이 정정하여 반영했습니다 (원본 문서 자체는 별도 정식 개정 필요, 각 페이지에 노란색 콜아웃으로 정정 근거를 명시):

| 대상 | 원본 표기 | QR 페이지 반영 내용 |
|---|---|---|
| 공정No.180 작업순서 6단계 | "조립전 전체계 리크 테스트 공정으로 이송한다" | "조립후 전체계 리크 테스트 공정으로 이송한다" |
| 공정No.190 작업순서 6단계 | "조립전 전체계 리크 테스트 공정으로 이송한다" | "테이퍼 플러그 돌출높이 검사(공정No.200) 공정으로 이송한다" |
| 공정No.200 설비명 | "PIN-DOWEL 압입기" | "TAPER PLUG 높이검사기" |

---

## 10. 버전 관리 (Git) 이력 요약

| 커밋 | 날짜 | 내용 |
|---|---|---|
| `486dc6b` | 2026-08-17 | 암호 게이트 인증 유지 방식을 30일 저장 → 탭/세션 단위 재인증으로 변경 |
| `5183c06` | 2026-08-17 | 암호 게이트를 "페이지 전체 차단" → "섹션 펼칠 때만 확인" 방식으로 전환, 암호 교체 |
| `71bbca0` | 2026-08-17 | 전 페이지에 간이 접속 암호 게이트(`gate.js`) 최초 추가 |
| `1140588` | 2026-07-30 | 홈 화면 제목을 "전체 설비 인덱스"로 변경 |
| `e024d6f` | 2026-07-30 | "공정 설비 정보 시스템" → "공정 설비 정보관리 시스템"(+영문) 전 페이지 일괄 변경 |
| `8a49c52` | 2026-07-30 | 기존 index.html → leak-pre-flow.html 이름 변경, 신규 홈 메뉴 페이지 추가, QR코드 11종 커밋 |
| `a399dbe` | 2026-07-29 | 설비 9개 페이지 신규 추가 (공정No.110~200) |
| `f098b7e` 이전 | 2026-07-29 이전 | 최초 저장소 클론 원본 (기존 index.html 1개 + PDF 4개) |

브랜치는 `main` 하나만 사용하며, 모든 변경사항은 로컬에서 커밋 후 바로 `origin/main`에 push하는 단순한 흐름입니다. 별도의 PR·리뷰 프로세스는 없습니다.

**실측 소요 시간** (커밋 타임스탬프 기준, 사내 원가절감 비교 자료에도 사용): 설비 10개소 + 홈 화면 + QR 전체 구축은 2026-07-29 오후~07-30 오전, 약 **1일** 소요. 접속 암호 게이트 추가는 2026-08-17 오전, 약 **20분** 소요.

---

## 11. 알려진 제한사항

- **접근 제어는 간이 수준**: §8 참고. 페이지 전체가 아니라 섹션 단위로, 공유 암호 1개로 보호되는 수준이며 실제 보안 장치는 아닙니다.
- **PDF 미완성**: 최초 4개(leak-pre-flow용)를 제외한 26개 PDF(§6)가 아직 업로드되지 않음. 링크는 걸려 있으나 클릭 시 404.
- **CSS/JS 중복**: 공용 스타일시트 파일이 없어 11개 파일 각각에 동일한 `<style>` 블록이 중복 포함됨 (`gate.js`만 유일하게 공용 파일). 디자인 변경 시 전 파일을 일일이 수정해야 함 (자동화 스크립트 없음).
- **빌드/테스트 없음**: 정적 HTML이라 별도 빌드·테스트 파이프라인이 없고, 배포 전 수동 확인에 의존.

## 12. 향후 계획

- [ ] 설비별 PART LIST·기계설명서·매뉴얼 PDF 첨부 완료 (§6 미업로드 항목)
- [ ] **신규 항목 추가**: 설비전기회로도·기계도면 등을 페이지에 추가 예정
- [ ] **항목별 접속 제한 세분화**: 위 신규 항목 추가 후, 어떤 섹션/항목을 암호로 잠글지 개별 결정 (§8 한계 참고)
- [ ] QR코드 인쇄 후 실제 설비에 부착
- [ ] 사내 개선제안서 제출 (외주 구축 대비 원가절감 비교자료 포함 — §13 참고)
- [ ] 타 라인·타 공정으로 확대 적용 검토
- [ ] 다국어(영/중) 페이지 지원 검토
- [ ] (검토) 개인 계정·역할별 열람이력이 필요해지면 별도 백엔드 도입 여부 결정 (현재는 인원 규모상 불필요 판단)

---

## 13. 외주 견적 대비 자체 구축 비교 (사내 개선제안서용)

외주업체(WISHKET CONSULTING, 2026-08-14자 제안서) 견적과 비교한 근거 자료. 상세 표는 대화 이력 및 `공정 설비 정보관리 시스템_구축과정 설명자료.docx` 참고.

**요약**:
- 외주 견적: 초기구축(기본형) 476~560만원 ~ (확장형) 1,194~1,404만원, 연 유지관리 36~180만원, 최소 납기 3주
- 자체 구축: 초기 0원, 유지관리 0원, 실제 소요 약 1일(§10) + 접속 제한 추가 20분
- 외주 "운영형"의 변경이력·접근제한 항목은 git 커밋 이력(§10)과 §8 암호 게이트로 이미 자체 충족
- 미충족 항목(개인계정·역할권한 등 "확장형")은 현재 사용 인원 규모(작업자 1~2명, 관리자 2명)상 필요성 낮다고 판단 — 외주 제안서 자체에도 동일하게 명시됨

---

## 14. 관련 산출물

| 파일 | 위치 | 용도 |
|---|---|---|
| `공정 설비 정보관리 시스템 도입 보고.pptx` | 바탕화면 | 사장님 보고용 PPT (10장) |
| `공정 설비 정보관리 시스템_구축과정 설명자료.docx` | 바탕화면 | 비개발자용 용어 설명 + Q&A |
| `qr-codes/*.png` | 프로젝트 폴더 내 | 설비별 QR코드 이미지 (11개) |
| `gate.js` | 프로젝트 폴더 내 | 접속 암호 게이트 스크립트 (§8) |
| `pdf-viewer.html`, `pdfjs/` | 프로젝트 폴더 내 | 다운로드 버튼 없는 PDF 뷰어 (§6) |
| 본 문서 (`PROJECT_OVERVIEW.md`) | 프로젝트 폴더 내 | 기술 참고 문서 (이 파일) |
